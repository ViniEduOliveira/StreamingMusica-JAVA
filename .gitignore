# Ignora todos os arquivos de ambiente reais
.env
.env.local

# MAS NÃO ignora o arquivo de exemplo (o ! inverte a regra)
!.env.example# Arquivos de ambiente e segredos
.env
.env.local
.env.dev
.env.test
